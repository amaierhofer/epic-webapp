- title "Sign up"

= render :partial => 'shared/credentials_formlistener'

%p You need a username and password to use this service. Read more at the #{link_to 'details', :about_technology, 'rel' => 'external' }.

= simple_form_for(@user) do |f| 
  = f.error_messages
  = f.input :name, :required => false
  = f.input :password, :required => false
  = f.submit "Sign up"

