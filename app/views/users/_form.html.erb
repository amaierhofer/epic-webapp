<%= form_for @user do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name, :placeholder => t(:name_hint, :scope => :user)%>
  </p>
  <p><%= f.submit %></p>
<% end %>
